# ── PyTorch ───────────────────────────────────────────────────
# Installed separately in Dockerfile via --index-url https://download.pytorch.org/whl/cu128
# to guarantee CUDA builds. Not listed here to avoid version constraint conflicts.

# ── HuggingFace ──────────────────────────────────────────────
# Pin to a version that includes Sam3TrackerModel / Sam3TrackerProcessor.
# Verify: python -c "from transformers import Sam3TrackerModel"
transformers>=4.57.0
accelerate>=0.34.0
safetensors>=0.4.0
huggingface_hub>=0.27.0

# ── Image processing ─────────────────────────────────────────
Pillow>=10.3.0
numpy>=1.26.0,<2.0.0

# ── Label Studio SDK ─────────────────────────────────────────
label-studio-ml>=1.0.9
label-studio-sdk>=1.0.0
# RLE encoding for BrushLabels (Label Studio custom format)
label-studio-converter>=0.0.57
# rq (Redis Queue) — used by label_studio_ml.api; not always pulled transitively
rq>=1.16.0

# ── HTTP client ──────────────────────────────────────────────
requests>=2.32.0

# ── Server ───────────────────────────────────────────────────
gunicorn>=22.0.0
